const swap = () => {};

const selectionSort = (arr, n) => {
    for(let i=0;i<n;i++){
        let min = arr[i]
        for(let j=i+1;j<n;j++){
            if(min<arr[j]){
                let temp = 
            }
        }
    }
};

let values = [10, 50, 30, 70, 80, 20, 90, 40];
console.log("Longest Substring", LongestSubstring(values, values.length));
